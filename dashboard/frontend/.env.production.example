# Production Environment Configuration
# Copy to .env.production and configure for your production environment

# Backend API URL (via nginx reverse proxy)
# Note: DOMAIN is set in dashboard/.env and passed via docker-compose
# For local production builds, replace with your actual domain:
VITE_API_URL=https://api.yourdomain.com

# Enable production optimizations
VITE_APP_ENV=production

# Optional: Analytics/monitoring
# VITE_ANALYTICS_ID=your_analytics_id

# Note: When using docker-compose.prod.yml, this is automatically
# set to https://api.${DOMAIN} where DOMAIN comes from dashboard/.env
