# Sports Data MCP - Production Dependencies
# ============================================
# Pin to specific minor versions for reproducibility.
# See requirements-dev.txt for development dependencies.
# 
# To generate a lockfile with pip-compile:
#   pip install pip-tools
#   pip-compile requirements.txt -o requirements.lock
#
# Python 3.11+ required

# Core MCP framework
mcp>=1.0.0,<2.0.0

# HTTP client for API requests (async)
aiohttp>=3.9.0,<4.0.0
aiofiles>=23.0.0,<24.0.0

# Environment configuration
python-dotenv>=1.0.0,<2.0.0
